package com.videoplatform.roomprofile;

/**
 * Classificacao do tipo de sala. Nesta sprint e' apenas informativo — nenhum
 * comportamento deriva do tipo (Sprint 5 §9). Facilmente extensivel.
 */
public enum RoomType {
    LESSON,
    CONSULTATION,
    MEETING,
    INTERVIEW,
    OTHER
}
